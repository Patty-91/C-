#include "pch.h"
#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{


//This will generate an error

//int size{};
//cout << "Enter the size of the array" << endl;
//cin >> size;

//int myArray[size];



int dynamicSize{};
cout << "Enter the size of the array" << endl;
cin >> dynamicSize;

int *myPtr{};
myPtr = new int[dynamicSize];

cout << "enter an element to the array" << endl;
cin >> myPtr[0];

cout << "enter an element to the array" << endl;
cin >> myPtr[1];

cout << "enter an element to the array" << endl;
cin >> myPtr[2];


cout << "These are my first three elements" << endl;


for (size_t i = 0; i < dynamicSize;i++) {
cout << myPtr[i] << endl;
}



}
